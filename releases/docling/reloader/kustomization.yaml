apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: infra-reloader
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: docling-reloader-values
    files:
      - values.yaml=values.json
