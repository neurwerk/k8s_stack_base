[project]
name = "k8s-stack-base-validation"
version = "0.0.0"
description = "Validation tooling for the Neurwerk platform release contract"
requires-python = ">=3.12"
dependencies = [
    "jsonschema>=4.25,<5",
    "pyyaml>=6.0,<7",
]

[tool.uv]
package = false
