# Platform v0.3.1

> BLOCKED DRAFT: The support and recovery declarations below record requested
> policy, not verified upgrade support. Do not merge, tag, publish, or adopt this
> proposal until every TODO evidence blocker is resolved and reviewed. See #71.

## Support

- Stable upgrades: Supported.
- Supported alpha source revisions: `aa3241882c2d8443af5a0823cf6f12caae90219d`.
- Downgrade: Unsupported.

The proposed stable policy covers all lower exact stable tags, currently
`v0.1.0`, `v0.1.1`, and `v0.3.0`, including skipped-version transitions.
The signed predecessor is `v0.3.0`; included implementation ends at
`1ee6085519f8f6898cb1f2bd6dd6cf9bc18da0e1` (routing PR #70).

TODO: Provide a tested cumulative in-place migration procedure for each lower
stable source. The immutable older migrations prohibit stable upgrades and do
not describe a tested cumulative path through the AgentGateway CRD, model
catalog, usage-database, and other crossed changes. Reading those files is
necessary but not sufficient; do not invent an intermediate checkpoint or
reinterpret a fresh-install prerequisite as an upgrade procedure.

TODO: Provide acceptance for the exact requested alpha source to this candidate.
The alpha SHA predates #70, so it does not establish acceptance of the routing
delta. Neither choosing this policy nor passing offline CI proves live support.

## Prerequisites

- Verify the signed predecessor and, only after separate publication, the exact
  target tag and independently authenticated platform signer. Provision public
  `k8s-stack-release-trust` outside Git.
- Use every exact prerequisite in `release/manifest.yaml`, unchanged from
  `v0.3.0`: Flux 2.9.4, openbao-stack-setup 0.2.11 at tooling commit
  `5d1a33a938e22e9034581aebecf33485adc88a29`, Gateway API experimental 1.6.0,
  cert-manager CRDs 1.20.2, Prometheus Operator chart/CRDs 72.4.0,
  AgentGateway CRDs 1.5.0, External Secrets CRDs 2.9.0, and the DocumentDB
  x86-64-v3 CPU baseline. For existing state, validate each prerequisite
  transition rather than blindly reinstalling or downgrading APIs.
- Verify successful schema-4 reconciliation with the exact declared
  openbao-stack-setup tool before enabling AgentGateway usage storage. The tool
  does not migrate application databases or authorize a platform transition.
- Preserve the required client-owned model catalog, pricing, Dify default,
  local fallback, roles, storage, certificates, and externally delivered secrets.
- TODO: Attach application-consistent independent backup, replacement restore,
  and integrity results covering every affected persistent component for the
  cumulative upgrade. Retained PVCs and in-cluster snapshots are not sufficient.

## Client Actions

These are proposed operator actions, not rollout authorization:

1. Review every crossed migration and breaking-change section in ascending
   SemVer order. Stop at the unresolved cumulative migration blocker above.
2. Render the complete client composition. Stage the routing mode in client
   values consumed by Dify API, LibreChat app, and Keycloak initial-admin before
   platform adoption. Use `public-dns` for public canonical endpoint resolution;
   use `internal-traefik` only with the reviewed internal-routing plan.
3. After evidence, publication, and separate adoption approval, select exact
   signed tag `v0.3.1`, matching adoption-target, and the reviewed adoption mode.
   Fresh installation instead requires an independently verified empty or
   replacement target. The current draft authorizes neither mode.
4. For internal routing, reconcile and verify the new NetworkPolicies before
   separately adding exact CoreDNS rewrites for the reviewed names. Never use
   wildcard or parent-zone rewrites. Client and platform sources are not atomic.

## Breaking Changes

- `canonicalEndpointRouting.mode` defaults to `internal-traefik`. Unknown modes
  fail Helm rendering. Public DNS clients must explicitly set `public-dns`.
- The Keycloak action-email Job defaults to selected Traefik Pod egress on TCP
  `443` instead of general public HTTPS. Without aligned DNS and values, issuer
  readiness and action-email delivery can fail. Dify API and LibreChat retain
  their existing public HTTPS allowances in both modes.
- Existing `v0.3.0` model-catalog and pricing requirements still apply. They must
  not be treated as proof that an older stable environment can migrate safely.

## Stateful And API Effects

The `v0.3.0`-to-candidate routing delta changes only the three chart patch
versions (`1.0.1` to `1.0.2`), routing-mode defaults, NetworkPolicies, and tests.
It changes no application image, database schema, CRD, Kubernetes identity, or
persistent volume. This statement is not true of every cumulative older-source
transition and does not supply that missing migration evidence.

Exact Pod DNS rewrites are cluster-wide for matching names, not workload scoped.
Canonical URLs, TLS SNI, OIDC issuer identity, redirects, and S3 signatures must
remain unchanged. Standard NetworkPolicy cannot restrict TLS SNI or HTTP Host on
the shared Traefik listener. A compromised allowed workload can attempt other
routes; application authentication and authorization remain required.

## Pre-Deployment Checks

1. Resolve all draft evidence blockers and required review before adoption.
2. Verify exact source and target provenance, migration applicability, image
   inventory, prerequisites, exclusions, and exception expiry dates.
3. Run `make check` and `make release-check` and render the effective client
   values. Offline validation does not prove backup integrity or a live upgrade.
4. Test the exact transition on a disposable environment with representative
   state, application-level readiness, and the proposed forward-fix recovery.
5. Verify canonical DNS answers, Traefik selectors and port reachability, strict
   TLS, certificates, and the independently verified backups before rollout.

Available implementation evidence: #70 Required CI succeeded at
https://github.com/neurwerk/k8s_stack_base/actions/runs/34093947989/job/101653244035.
The rendered routing tests cover both modes and invalid-mode rejection. No live
transition, restore, or recovery was performed during this preparation.

## Post-Deployment Checks

After separately authorized deployment, record the exact applied revisions and:

1. Verify Flux sources, Kustomizations, and every enabled HelmRelease are Ready.
   Inspect warning events and affected logs without secret values or request data.
2. Verify affected policies select only the intended Traefik Pods on TCP `443`
   in internal mode, and omit the added allowance in public mode.
3. Verify exact rewritten DNS names and unrelated answers, strict HTTPS,
   Gateway/HTTPRoute acceptance and references, certificates, unchanged OIDC
   issuer identity, Dify and LibreChat authentication, and Keycloak action email.
4. Verify LibreChat signed S3 requests and affected application data, model
   authorization, streaming requests, and Studio usage attribution. Controller
   readiness alone does not prove data usability.

## Recovery

Recovery classification: Forward fix.

TODO: Supply the exact compatible repair revision, failure scenarios, tested
application/data integrity outcomes, and stop/escalation criteria for forward-fix
recovery from each supported source. No tested repair revision is available in
the reviewed evidence. The classification alone is not a recovery procedure.

If integrity is uncertain, stop rollout and preserve redacted failure evidence.
Do not downgrade a tag, database, or CRD, delete persistent data, or claim that
reverting a NetworkPolicy restores state. Any client DNS recovery must be
reviewed through Git and retain working resolver readiness; this alone is not
complete platform recovery. Recovery operations require separate authorization.

## Exclusions

- Downgrades remain unsupported.
- LibreChat RAG and Code Interpreter remain excluded from the release packages.
- Existing immutable LibreChat development-image and DocumentDB privilege
  exceptions retain their manifest owners and expiry dates (2026-09-30 and
  2026-11-27 respectively); this proposal does not renew or broaden them.
- K3s/control-plane upgrades, credential provisioning, cluster access, DNS
  changes, client adoption, deployment, tagging, and publication are outside
  this preparation. No live evidence is implied by these instructions.
