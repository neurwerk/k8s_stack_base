# Platform v0.3.7

## Support

- Stable upgrades: Supported.
- Supported alpha source revisions: None.
- Downgrade: Unsupported.

The stable policy permits strictly forward exact stable tags, including
`v0.3.6` to `v0.3.7`; review all crossed migrations when skipping versions.
Fresh installation requires a verified empty or replacement environment.
The permanent alpha environment is not being promoted to stable.

This draft depends on #152 and records its unmerged CPU adoption commit as
`includedThrough`. After the dependency merges, refresh provenance against the
actual main history, regenerate the manifest, retarget the release PR to main,
and rerun full checks and Required CI before final review. Publication and
client adoption require separate authorization; neither is established here.

## Breaking Changes

See the release notes in CHANGELOG.md for breaking changes and required actions.

Review application-admin inheritance and any subtract-only exclusions before
the transition. Coordinate separate platform/client sources, verify role
provisioning and fresh claims, and only then perform separately authorized
cleanup of redundant memberships. Preserve explicit model/MCP grants.

For an already-provisioned `v0.3.6` client selecting only eligible packages,
baseline prerequisites remain unchanged; no blanket OpenBao reconciliation is
required. Optional Forgejo/WireGuard catalogs are conditional on separately
approved selection and do not make excluded packages eligible. Maintenance
remains optional, disabled by default and excluded, with no new secret catalog.

Before adoption, verify independent application-consistent backups and restore
evidence, test the exact transition and recovery in a disposable environment,
and review the unchanged LibreChat exception expiry (`2026-09-30`) and #108's
accepted interactive-chat limitation. After an authorized rollout, inspect Flux
sources, Kustomizations, HelmReleases, warnings, affected applications, persistent
data and safe logs. Verify PostgreSQL provisioning/worker isolation, bridge
authorization, Studio startup and PII/model-sync behavior. These operational
checks have not been performed for this draft.

## Recovery

Recovery classification: Forward fix.

Do not downgrade persistent state or treat a Git/Helm revert as a restore.
Preserve complete authentication and operations PostgreSQL instances, OpenBao
and application credentials, API-key storage, PII state and authoritative model
objects. Optional Forgejo shares the operations database recovery domain and
also needs its retained PVC and durable secrets. If a forward fix is unsafe,
stop and use a separately reviewed component/replacement restore procedure with
verified independent backups; no restore is proven by this preparation.

For separately selected optional packages, stop WireGuard before peer, key or
target changes and recover with no peers when the approved list is untrusted.
Native Forgejo credentials require separate offboarding. Deactivate maintenance
scopes and complete operator-resource cleanup before removing static resources;
Flux pruning does not remove operator-owned routes or restore application data.
