# AgentGateway Tests

Future app-scoped tests:

- Model catalog rendering and public/internal visibility.
- Provider backend and Secret reference contracts.
- Gateway listener and model route compatibility.

Cross-chart identity, MCP authorization, and secret-exposure checks belong in `tests/security/`.
