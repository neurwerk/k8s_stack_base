apiVersion: v1
kind: Service
metadata:
  name: frontend-dify-sandbox-service
  namespace: frontend-dify
  labels:
    app.kubernetes.io/name: sandbox
    app.kubernetes.io/instance: frontend-dify-sandbox
    app.kubernetes.io/part-of: frontend-dify
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.frontendDify.sandbox.ports.service.http }}
      targetPort: {{ .Values.frontendDify.sandbox.ports.container.http }}
      protocol: TCP
  selector:
    app.kubernetes.io/name: sandbox
    app.kubernetes.io/instance: frontend-dify-sandbox
