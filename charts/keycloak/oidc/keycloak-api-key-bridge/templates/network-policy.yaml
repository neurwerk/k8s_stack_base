apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: auth-keycloak-keycloak-api-key-bridge-oidc-network-policy
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: keycloak-api-key-bridge-oidc
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/part-of: auth-keycloak
spec:
  podSelector:
    matchLabels:
      app: auth-keycloak-keycloak-api-key-bridge-oidc-job
  policyTypes: [Egress]
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: auth-keycloak
          podSelector:
            matchLabels:
              app: auth-keycloak-keycloak-app
      ports:
        # The Keycloak Service maps port 80 to Pod port 8080.
        - port: 8080
          protocol: TCP
        - port: 9000
          protocol: TCP
