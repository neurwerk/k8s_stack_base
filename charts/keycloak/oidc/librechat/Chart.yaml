# =============================================================================
# Chart: keycloak-oidc-configurations/librechat
#
# Post-install Job that converges the LibreChat OIDC client.
# =============================================================================
apiVersion: v2
name: librechat
description: LibreChat OIDC client configuration in Keycloak
type: application
version: 1.0.0
appVersion: "1.0"
